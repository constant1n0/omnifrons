{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: default core functionality only, no fs, shell, http, or dialog access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
